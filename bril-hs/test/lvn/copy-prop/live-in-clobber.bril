@main {
  x: int = const 0;
.lbl:
  # Looking up id x returns VN 1
  # var2num[y] = 1
  y: int = id x;
  x: int = const 1;
  print y;
}

@foo(a: int, b: int) {
  temp: int = id b;	
  one: int = const 1;
  b: int = id one;	
  v11: int = id temp;	
}