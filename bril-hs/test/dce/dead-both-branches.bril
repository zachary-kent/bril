@main {
  # This def is dead because i is reassigned in both paths
  i: int = const 0;
  cond: bool = const true;
  br cond .hi .bye;
.hi:
  i: int = const 1;
  jmp .end;
.bye:
  i: int = const 2;
.end:
  print i;
}