extract = 'total_dyn_inst: (\d+)'
benchmarks = '../benchmarks/*/*.bril'

[runs.baseline]
pipeline = [
    "bril2json",
    "brili -p {args}",
]

[runs.myopt]
pipeline = [
    "bril2json",
    "stack run -- --dce --lvn",
    "brili -p {args}",
]